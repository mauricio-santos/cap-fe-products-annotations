_schema-version: 3.3.0
ID: sap-cap-fe
description: A simple CAP project.
version: 1.0.0
modules:
- name: sap-cap-fe-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: sap-cap-fe-auth
  - name: sap-cap-fe-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: sap-cap-fe-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: sap-cap-fe-db
  parameters:
    buildpack: nodejs_buildpack
- name: sap-cap-fe-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: sap-cap-fe-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - productscapcds.zip
      name: productscapcds
      target-path: resources/
- name: productscapcds
  type: html5
  path: app/products-cap-cds
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: sap-cap-fe-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: sap-cap-fe-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: sap-cap-fe-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: sap-cap-fe-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: sap-cap-fe-html5-service
    service-plan: app-host
- name: sap-cap-fe-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: sap-cap-fe-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
